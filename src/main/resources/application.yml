server:
  port: 8085
spring:
  application:
    name: lover
  datasource:
    username: root
    password: .
    url: jdbc:mysql://localhost:3306/lover?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mright.lover.platform.entity
